output {
    elasticsearch {
        hosts => "${ELASTICSEARCH_HOSTS:elasticsearch:9200}"
        index => "${ELASTICSEARCH_INDEX:jmeter}-%{+YYYY.MM.dd}"
        http_compression => "${ELASTICSEARCH_COMPRESSION:false}"
        user => "${ELASTICSEARCH_USER:}"
        password => "${ELASTICSEARCH_PASSWORD:}"
    }
}